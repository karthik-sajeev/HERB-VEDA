<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <style>
        body {
            background-color: #121212; /* Dark background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #ffffff; /* White text color */
        }
        .login-container {
            background-color: #1f1f1f; /* Darker background for the container */
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            width: 400px;
            padding: 30px;
        }
        .login-container h2 {
            margin-bottom: 20px;
            color: #f1f1f1; /* Light gray text */
            text-align: center;
        }
        .login-container img {
            width: 100px; /* Icon width */
            display: block;
            margin: 0 auto 20px; /* Centering the icon */
        }
        .form-control {
            border-radius: 50px; /* Rounded corners */
            padding: 15px;
            margin-bottom: 15px;
            background-color: #333333; /* Dark input background */
            border: 1px solid #444444; /* Lighter border */
            color: #ffffff; /* White text inside inputs */
        }
        .form-control:focus {
            background-color: #555555; /* Focused input */
            border-color: #007bff; /* Border color on focus */
        }
        .btn-primary {
            background-color: #007bff; /* Primary button color */
            border: none;
            border-radius: 50px; /* Rounded corners */
            padding: 10px 20px;
            width: 100%;
            transition: background-color 0.3s;
        }
        .btn-primary:hover {
            background-color: #0056b3; /* Darker shade on hover */
        }
        .error-message {
            color: #ff4d4d; /* Red error message */
            text-align: center;
            margin-bottom: 15px;
        }
        #formFooter {
            text-align: center;
            margin-top: 15px;
        }
        #formFooter a {
            color: #007bff;
            text-decoration: none;
        }
        #formFooter a:hover {
            text-decoration: underline;
        }
        input[type="password"] {
    color: #f1f1f1; /* Light gray text inside password field */
}
    </style>
</head>
<body>
    <div class="login-container">
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRvts5aHBstDkR8PigS4RmZkbZy78zpZoSuOw&s" alt="User Icon" />
        <h2>Admin Login</h2>

        <% if (errorMessage) { %>
            <div class="error-message"><%= errorMessage %></div>
        <% } %>

        <form action="/admin/login" method="POST">
            <input type="text" class="form-control" name="email" placeholder="Email" required>
            <input type="password" class="form-control" name="password" placeholder="Password" required>
            <button type="submit" class="btn btn-primary">Log In</button>
        </form>

        <div id="formFooter">
            <a href="/admin/forgot-password">Forgot Password?</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
